// To check whether the num is divisible or not?

#include <stdio.h>

int main(){
    int num;

    printf("Enter the value \n");
    scanf("%d",&num);

    printf("Divisibility test returns : %d", num%97);
     return 0;
}